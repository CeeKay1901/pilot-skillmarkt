# Beispiel-Ausgabe

So sieht ein typisches Ergebnis von `/code-check` aus:

```
Check deines Tools "tkp-rechner.html"

✓ Funktioniert korrekt (5 Testfälle geprüft)
⚠ Verbesserung: Bei leerem Feld erscheint "NaN"
   → Vorschlag: freundliche Meldung statt Fehler
💡 Tipp: Ergebnis ließe sich als PDF exportieren

Kein kritisches Problem — dein Tool ist einsatzbereit.
```
